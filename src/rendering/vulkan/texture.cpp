#include "rendering/vulkan/texture.h"

namespace chove::rendering::vulkan {
Texture* Texture::CreateTexture(const Context& context, VmaAllocator allocator, const std::filesystem::path& path) {
  return nullptr;
}
} // namespace chove::rendering::vulkan
